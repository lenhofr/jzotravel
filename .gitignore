node_modules/
dist/

# Compiled from content/blog/*.md by scripts/generate-posts.mjs on every
# build and dev start. Committing it would make every content edit produce a
# second, conflict-prone diff. After a fresh clone, run `npm run generate:posts`
# once so the import in src/data/posts.ts resolves in your editor.
src/data/posts.generated.json

# Terraform
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfplan
.terraform.lock.hcl
tfapply.log
plan_raw.log
plan.txt
plan.json
init.log
validate.log

# Local env
.env
.env.local

# Local dev artifacts
tmp_artifacts/
.vite/

# Mac Finder
.DS_Store